name: itunes
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+2
  sqflite: ^1.1.6+3
  path_provider: ^1.2.0
  intl: ^0.15.8
  rxdart: ^0.22.0
  shared_preferences: ^0.5.3+4
  page_transition: "^1.1.5"
  video_player: ^0.10.2+5
  dio: "1.0.0"
  flutter_playout: ^1.0.11

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
   - assets/
